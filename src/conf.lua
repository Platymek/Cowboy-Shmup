
conf = {

    p = { -- player
    
        stunRel = 0.75, -- stun on reload
        cancRel = 0.5,  -- cancel reload duration

        stunSho = 0.3,  -- stun duration on shoot
        cancSho = 0.2,  -- cancel reload duration

        speed   = 32,
        maxAmmo = 6,
        buff    = 0.4,  -- buffer duration
        health  = 3,
        hRadius = 2,    -- hit radius
    },

    aimOff = 0.075, -- aim offset angle
}
